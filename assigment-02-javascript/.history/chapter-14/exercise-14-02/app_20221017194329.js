const myRange = document.getElementById('myRange');
const content = document.getElementById('content');
let ncols = myRange.value;

myRange.addEventListener('click', (e) => {
    console.log(myRange.value);
})

