const limitFunc = (fn, limit) => (function(){
    let counter = 0;
    if(counter < limit){
        return () = con
    }
})

const limited = limitFunc(() => {
    console.log('hi')
}, 2)

limited()
limited()